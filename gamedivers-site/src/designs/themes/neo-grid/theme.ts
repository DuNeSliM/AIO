import type { DesignManifest } from '../../themeManifest'

const neoGridManifest: DesignManifest = {
  layout: 'default',
  assets: {
    panelTexture: `linear-gradient(180deg, rgba(72, 242, 255, 0.06), transparent 38%)`,
    buttonTexture: `linear-gradient(135deg, rgba(72, 242, 255, 0.2), rgba(255, 122, 0, 0.1))`,
    sidebarTexture: `linear-gradient(180deg, rgba(72, 242, 255, 0.1), rgba(255, 122, 0, 0.06))`,
  },
}

export default neoGridManifest
