apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: go-api
spec:
  ingressClassName: traefik
  rules:
    - host: gamedivers.de
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: go-api
                port:
                  number: 80
