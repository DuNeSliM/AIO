apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gamedivers-api
  namespace: gamedivers
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-gamedivers
spec:
  ingressClassName: traefik
  tls:
    - hosts: [gamedivers.de]
      secretName: letsencrypt-gamedivers-account-key
  rules:
    - host: gamedivers.de
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: gamedivers-api
                port:
                  number: 80
