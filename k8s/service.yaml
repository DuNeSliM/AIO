# service.yaml
apiVersion: v1
kind: Service
metadata:
  name: go-api
  namespace: go-api
  labels:
    app: go-api
spec:
  type: ClusterIP
  selector:
    app: go-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
