apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gamedivers-website
spec:
  ingressClassName: traefik
  rules:
    - host: gamedivers.de
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: gamedivers-website
                port:
                  number: 80
